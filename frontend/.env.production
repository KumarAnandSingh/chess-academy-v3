# Production environment configuration for Chess Academy
# Set VITE_BACKEND_URL in your hosting provider (e.g. Vercel) to the live Socket.IO backend.
# The value below is the actual Render.com backend URL
VITE_BACKEND_URL=https://chess-academy-backend-o3iy.onrender.com

# Environment mode
VITE_APP_ENV=production

# Build configuration
VITE_BUILD_TARGET=production

# Optimization flags
VITE_OPTIMIZE_DEPS=true
VITE_MINIFY=true

# Chess Academy specific configuration
VITE_APP_NAME=Chess Academy
VITE_APP_VERSION=1.0.0
VITE_PLATFORM=render
